ANTHROPIC_API_KEY=sk-ant-...
GARAK_API_KEY=

# Docker Compose로 backend 실행 시: 반드시 db (서비스명). localhost면 DB 연결 실패.
# 호스트에서 db/load_data.py만 돌릴 때: localhost
DB_HOST=db
DB_PORT=5432
DB_NAME=sosang
DB_USER=sosang
DB_PASSWORD=sosang
